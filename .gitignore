# 运行数据 / 凭据（含明文 API Key，禁止提交、上传、分享）
.dshw-data/
credentials.json

# WebView2 运行时缓存
host.exe.WebView2/
wv2-profile/

# 日志
*.log

# 下载的 WebView2 SDK 包 / 完整 SDK（体积大，仅重新编译时才需要）
*.zip
*.nupkg
webview2-sdk/
wv2-sdk/
